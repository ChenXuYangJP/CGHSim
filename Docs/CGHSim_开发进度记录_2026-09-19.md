# CGHSim 开发进度记录

最后更新：2026-09-21。第 1–11 节保留各阶段的历史记录；最新实现与验证见第 12 节，SLM 像素坐标约定与代码审查见第 13 节。续接开发以 [开发交接文档](CGHSim_Development_Handoff.md) 为入口。

最初记录日期：2026-09-19

项目方向：全息影像模拟

当前阶段（2026-09-21）：已具备 CGH Actor 场景、SI 场景快照、网格目标及截面轮廓点云、版本化资源缓存，以及 SLM 相位存储和选中时的原生编辑器预览。Editor/Game 构建、21 项无界面自动化测试和 1 项 Vulkan/Slate 实际渲染测试均通过；光学求解、Cook/打包及完整人工验收仍未完成。

验证依据包括终端输出、构建截图、自动化与实际渲染测试报告，以及用户运行反馈。本文按日期保留已确认结果；早期章节中的“当前”“尚未实现”等表述对应当时状态，最新状态以第 12 节为准。

## 1. 当前已达到的里程碑

用户最新确认：

> 目前在 remote vs code 里启动，moonlight 里能看到远程桌面上的 ue editor 启动了。

已验证的操作链路为：

```text
本地 VS Code
    ↓ Remote SSH
Ubuntu 主机上的 CGHSim 开发环境
    ↓ 启动 UE Editor
Ubuntu 图形桌面
    ↓ Moonlight 查看远程桌面
本地能够看到 UE Editor 窗口
```

这次确认覆盖了远程启动和窗口可见性。C++ 断点调试、Simulation 启动项、关卡内功能以及打包后的独立应用，尚未分别完成验证记录。

## 2. 项目目标与开发方案

计划从空白 C++ UE 项目起步，逐步加入 assets、仿真逻辑和外部接口，形成全息影像模拟应用。希望能够在 VS Code 中完成代码编辑和构建，并最终生成可独立部署的应用。

当前采用 VS Code Remote SSH 操作 Ubuntu 上的项目，使用 Moonlight 查看和操作远程图形桌面。引擎与项目均保留在 Ubuntu 主机上。

此前讨论过配合 ChatGPT/Codex 或 Claude 的 AI 编程流程；尚无 AI 工具安装、授权或实际项目使用的确认记录。

此前教程中的 `HoloSim` 为示例名。当前实际项目名称为 `CGHSim`，后续命令和配置应使用当前名称及实际路径。

## 3. 已知环境与路径

| 项目 | 当前记录 |
| --- | --- |
| 远程系统 | Ubuntu，具体发行版本未记录 |
| 主机名 | `cxy-MZ32-AR0-00` |
| 开发账号 | `cxy` |
| 用户 UID | `1000`，已通过 `id -u` 确认 |
| Unreal Engine 版本 | `5.8`，由用户明确提供 |
| 引擎根目录 | `/home/cxy/opt/UnrealEngine/UE5` |
| 编辑器可执行文件 | `/home/cxy/opt/UnrealEngine/UE5/Engine/Binaries/Linux/UnrealEditor` |
| 项目根目录 | `/home/cxy/Documents/Unreal Projects/CGHSim` |
| 项目文件 | `/home/cxy/Documents/Unreal Projects/CGHSim/CGHSim.uproject` |
| 已成功生成的 Game 可执行文件 | `/home/cxy/Documents/Unreal Projects/CGHSim/Binaries/Linux/CGHSim` |
| 项目工作区文件的约定位置 | `/home/cxy/Documents/Unreal Projects/CGHSim/CGHSim.Dev.code-workspace` |
| 图形环境文件的约定位置 | `/home/cxy/Documents/Unreal Projects/CGHSim/.ue-desktop.env` |
| 远程代码入口 | VS Code Remote SSH |
| 远程图形入口 | Moonlight |

项目路径中的 `Unreal Projects` 含空格。Shell 命令中的项目路径应始终使用引号，JSON 中则作为完整字符串保存。

显卡型号、驱动版本、Ubuntu 具体版本和编译器版本尚未归档，不在此处推定。

## 4. 构建与启动问题处理记录

### 4.1 UE 5.8 安装包中没有 SetupToolchain.sh

用户实际列出的 `Engine/Build/BatchFiles/Linux/` 文件为：

```text
Build.sh
FixDependencyFiles.sh
GenerateLLDBInit.sh
QASmokeManual.sh
SetupEnvironment.sh
dpkg-query.tps
GenerateGDBInit.sh
GenerateProjectFiles.sh
SetupDotnet.sh
Setup.sh
```

此前建议执行的 `SetupToolchain.sh` 不适用于这份安装包，相关步骤已撤回。没有将目录中的其他 `Setup` 脚本直接视为替代品。

后续已经出现成功的项目构建结果，因此当前流程应从已能构建和启动的状态继续。编译器具体版本尚未通过用户输出单独记录，不沿用此前未经本机验证的版本推断。

### 4.2 CGHSim 的 Game 目标已构建成功

截图中的构建任务使用了：

```text
CGHSim Linux Development
```

截图包含以下成功输出：

```text
[3/4] Link CGHSim
[4/4] WriteMetadata CGHSim.target [NoUba]

Output binary: /home/cxy/Documents/Unreal Projects/CGHSim/Binaries/Linux/CGHSim

Result: Succeeded
Total execution time: 54.27 seconds
```

这条记录确认了项目 C++ 的一次 Game 目标构建。它没有同时证明资源 Cook、打包或独立应用运行已经完成。

### 4.3 旧启动配置指向了不存在的 UnrealGame

VS Code 当时尝试启动：

```text
/home/cxy/Documents/Unreal Projects/CGHSim/Binaries/Linux/UnrealGame
```

实际构建产物为：

```text
/home/cxy/Documents/Unreal Projects/CGHSim/Binaries/Linux/CGHSim
```

当时的直接问题是启动路径与实际构建产物不一致。这个报错发生在成功编译之后。

随后为日常开发提供了新的工作区方案，统一构建 `CGHSimEditor Linux Development`，并使用引擎中的 `UnrealEditor` 启动项目。

### 4.4 当前约定的两种开发启动配置

| 启动配置 | 预启动任务 | 启动程序 | 主要参数 |
| --- | --- | --- | --- |
| `CGHSim Dev: Unreal Editor` | `CGHSim Dev: Build Editor` | 引擎中的 `UnrealEditor` | 项目文件、`-log` |
| `CGHSim Dev: Simulation` | `CGHSim Dev: Build Editor` | 引擎中的 `UnrealEditor` | 项目文件、`-game`、窗口参数 |

两项配置都应分别引用同一个 `.ue-desktop.env`。只在其中一项添加 `envFile`，不会修改另一项的配置内容。

当前已确认 UE Editor 能通过 Remote VS Code 启动，并在 Moonlight 中看到窗口。尚未单独记录 `CGHSim Dev: Simulation` 的运行结果，也没有保存本次 Editor 目标的完整构建日志。

## 5. 图形会话环境与验证结果

### 5.1 用户实际生成的环境内容

```dotenv
DISPLAY=:0
XDG_RUNTIME_DIR=/run/user/1000
XAUTHORITY=/run/user/1000/gdm/Xauthority
DBUS_SESSION_BUS_ADDRESS=unix:path=/run/user/1000/bus
```

约定保存在：

```text
/home/cxy/Documents/Unreal Projects/CGHSim/.ue-desktop.env
```

此前采用的获取方法是在 Remote SSH 终端读取 `systemctl --user show-environment`，筛选图形会话相关字段后写入文件。这份快照没有 `WAYLAND_DISPLAY`，当前已成功显示编辑器，不需要为本次已成功的配置补填猜测值。

### 5.2 用户已确认的基础检查

```bash
id -u
```

实际输出：

```text
1000
```

授权文件检查：

```bash
if test -r /run/user/1000/gdm/Xauthority; then
    echo "Xauthority 文件可读"
else
    echo "Xauthority 文件不存在，或当前用户无法读取"
fi
```

实际输出：

```text
Xauthority 文件可读
```

随后用户确认能够在 Moonlight 中看到从 Remote VS Code 启动的编辑器。至此，除了基础权限检查，还获得了图形程序实际启动的反馈。

### 5.3 工作区引用方式

在两个启动配置对象中分别添加下面这一行，与 `program`、`args`、`preLaunchTask` 同一级：

```json
"envFile": "/home/cxy/Documents/Unreal Projects/CGHSim/.ue-desktop.env"
```

该文件属于当前主机和当前桌面会话的配置。后续重新登录桌面、切换账号或图形会话后，应重新检查实际环境值，不把当前快照视为永久固定参数。

## 6. 工作区参考快照

下面的 JSON 以此前提供的 `CGHSim.Dev.code-workspace` 模板为基础，补入两个启动配置的 `envFile`，用于恢复配置或与远程文件对照。远程主机上最终修改后的完整工作区文件尚未回传，因此本节不代表已逐项核对过远程文件。

使用此配置时，调试类型为 `lldb`，对应此前约定的 CodeLLDB 扩展。环境文件路径应与实际保存位置一致。

```json
{
  "folders": [
    {
      "name": "CGHSim",
      "path": "/home/cxy/Documents/Unreal Projects/CGHSim"
    },
    {
      "name": "UE5",
      "path": "/home/cxy/opt/UnrealEngine/UE5"
    }
  ],
  "extensions": {
    "recommendations": [
      "ms-vscode.cpptools",
      "ms-dotnettools.csharp",
      "vadimcn.vscode-lldb"
    ]
  },
  "tasks": {
    "version": "2.0.0",
    "tasks": [
      {
        "label": "CGHSim Dev: Build Editor",
        "type": "process",
        "command": "/bin/bash",
        "args": [
          "/home/cxy/opt/UnrealEngine/UE5/Engine/Build/BatchFiles/Linux/Build.sh",
          "CGHSimEditor",
          "Linux",
          "Development",
          "-Project=/home/cxy/Documents/Unreal Projects/CGHSim/CGHSim.uproject",
          "-WaitMutex"
        ],
        "options": {
          "cwd": "/home/cxy/opt/UnrealEngine/UE5"
        },
        "group": {
          "kind": "build",
          "isDefault": true
        },
        "problemMatcher": [
          "$gcc"
        ],
        "presentation": {
          "reveal": "always",
          "panel": "dedicated"
        }
      }
    ]
  },
  "launch": {
    "version": "0.2.0",
    "configurations": [
      {
        "name": "CGHSim Dev: Simulation",
        "type": "lldb",
        "request": "launch",
        "program": "/home/cxy/opt/UnrealEngine/UE5/Engine/Binaries/Linux/UnrealEditor",
        "args": [
          "/home/cxy/Documents/Unreal Projects/CGHSim/CGHSim.uproject",
          "-game",
          "-log",
          "-windowed",
          "-ResX=1280",
          "-ResY=720"
        ],
        "cwd": "/home/cxy/Documents/Unreal Projects/CGHSim",
        "preLaunchTask": "CGHSim Dev: Build Editor",
        "stopOnEntry": false,
        "terminal": "integrated",
        "envFile": "/home/cxy/Documents/Unreal Projects/CGHSim/.ue-desktop.env"
      },
      {
        "name": "CGHSim Dev: Unreal Editor",
        "type": "lldb",
        "request": "launch",
        "program": "/home/cxy/opt/UnrealEngine/UE5/Engine/Binaries/Linux/UnrealEditor",
        "args": [
          "/home/cxy/Documents/Unreal Projects/CGHSim/CGHSim.uproject",
          "-log"
        ],
        "cwd": "/home/cxy/Documents/Unreal Projects/CGHSim",
        "preLaunchTask": "CGHSim Dev: Build Editor",
        "stopOnEntry": false,
        "terminal": "integrated",
        "envFile": "/home/cxy/Documents/Unreal Projects/CGHSim/.ue-desktop.env"
      }
    ]
  }
}
```

## 7. 可复用命令

这些命令用于后续复查和恢复操作。已确认的执行结果以本文前面的日志与用户反馈为准。

### 7.1 构建 Editor 目标

在 Ubuntu 或 VS Code Remote SSH 终端执行：

```bash
UE_ROOT="/home/cxy/opt/UnrealEngine/UE5"
PROJECT="/home/cxy/Documents/Unreal Projects/CGHSim/CGHSim.uproject"

(
    cd "$UE_ROOT" || exit 1

    bash Engine/Build/BatchFiles/Linux/Build.sh \
        CGHSimEditor \
        Linux \
        Development \
        -Project="$PROJECT" \
        -WaitMutex
)
```

### 7.2 绕开调试器，直接测试编辑器启动

排查 F5 调试流程时，可在 Remote SSH 终端显式加载图形环境并直接启动：

```bash
(
    cd "/home/cxy/Documents/Unreal Projects/CGHSim" || exit 1

    set -a
    source ./.ue-desktop.env || exit 1
    set +a

    "/home/cxy/opt/UnrealEngine/UE5/Engine/Binaries/Linux/UnrealEditor" \
        "$PWD/CGHSim.uproject" \
        -log
)
```

### 7.3 开发期 Simulation 启动测试

下面是下一阶段可单独验证的启动方式，当前尚无成功运行反馈：

```bash
(
    cd "/home/cxy/Documents/Unreal Projects/CGHSim" || exit 1

    set -a
    source ./.ue-desktop.env || exit 1
    set +a

    "/home/cxy/opt/UnrealEngine/UE5/Engine/Binaries/Linux/UnrealEditor" \
        "$PWD/CGHSim.uproject" \
        -game \
        -log \
        -windowed \
        -ResX=1280 \
        -ResY=720
)
```

这条命令仍使用当前引擎安装目录中的可执行文件。最终独立部署的验收应单独针对打包输出进行。

## 8. 当前状态与后续验收

| 事项 | 状态 | 依据或下一步 |
| --- | --- | --- |
| UE 5.8 能够启动 | 已确认 | 用户在 Moonlight 中看到编辑器窗口 |
| VS Code Remote SSH 操作远程项目 | 已确认 | 用户在远程 VS Code 中执行构建、检查和启动 |
| CGHSim Game 目标 C++ 构建 | 已确认 | 截图中 `Result: Succeeded` 及 `CGHSim` 输出路径 |
| 图形会话 UID 和 Xauthority 可读性 | 已确认 | 用户返回 UID 1000 和授权文件可读 |
| Remote VS Code 启动后窗口在 Moonlight 可见 | 已确认 | 用户最新反馈 |
| Editor 构建完整日志归档 | 待补充 | 工作区已约定构建目标，尚未保存本次完整输出 |
| CodeLLDB 断点、变量查看、单步调试 | 待验证 | 设置一个 C++ 断点并确认实际命中 |
| `CGHSim Dev: Simulation` 运行 | 待验证 | 单独选择该启动项，检查窗口和关卡内容 |
| 测试关卡、相机、默认启动地图 | 待确认 | 此前提供过方案，用户尚未反馈完成情况 |
| Linux Cook、打包和独立启动 | 待验证 | 生成打包输出并在无需编辑器参与的流程中测试 |
| Git 基线与忽略规则 | 待确认 | 尚无仓库初始化和提交记录 |
| AI 编程工具接入 | 待实施或待确认 | 尚无安装、授权及修改项目的结果 |
| 全息仿真算法、assets、输入输出接口 | 待开发 | 尚未记录具体实现或设备协议 |

下一阶段建议先保存当前可运行状态，建立一个包含可见物体和相机的最小测试关卡，并确认默认地图。随后验证 Simulation 和一个 C++ 断点，再完成一次 Linux 打包与独立启动测试。

上述基础流程稳定后，再增加第一个仿真参数或外部接口，用它控制场景对象或材质，逐步接入全息影像模拟内容。具体算法、显示设备、输入数据格式和通信协议仍需后续明确。

## 9. 续接开发时保留的注意事项

继续使用实际项目名 `CGHSim` 和本记录中的项目路径，避免混入此前 `HoloSim` 示例路径。旧配置中的 `Binaries/Linux/UnrealGame` 与已观察到的构建产物不匹配。

不要把成功构建 Game 可执行文件、成功启动编辑器、成功打包独立应用合并为一个已完成事项。目前前两项有确认记录，独立应用尚未验收。

当前图形会话已经能显示编辑器，后续优先保持这组配置；出现新故障时，记录所选启动项、终端日志、是否能直接运行，以及 Moonlight 中是否可见窗口，再进行针对性排查。

本记录建议保存在项目的 `Docs` 目录，并在后续完成关键验证时更新。它不包含授权文件内容、账号口令或 API 密钥。


## 10. CGH 基础 Actor 与实验台场景（2026-09-19 补充）

已根据共享设计实现 `Source/CGHSim/CGH` 下的五个 C++ Actor、`CGHTypes.h` 和 `CGHUnitConversion.h`，并生成实际的 Blueprint `.uasset` 和 `L_CGHWorkbench.umap`。此前章节记录的是本次开发前的状态；本节作为更新后的验证依据。

| 对象 | 本次实现 |
| --- | --- |
| Target | 点目标振幅、初始相位；独立显示标记；导出相对 SLM 的米制坐标 |
| SLM | 分辨率、像素间距、派生有效尺寸、YZ 平面显示；状态保持 `NotImplemented` |
| Camera | 双精度光学参数单向同步至 Cine Camera 几何预览；独立 `OpticalReference` |
| Reconstruction Light | 波长、振幅、相位、偏振及 +X 传播方向；不充当 UE 场景照明 |
| Workbench | 显式关联 SLM/Camera/Light/Targets；Details 面板验证与刷新按钮 |

约定保持 UE 场景厘米单位，SLM 局部 +X 为光轴、+Y 为横向、+Z 为纵向；光学 Actor 根缩放保持 `(1,1,1)`。默认 SLM 的 `1024 × 1024`、`8 um` 对应 `8.192 × 8.192 mm`。目标标记大小不改变数学点参数。

已生成 `Content/CGHSim/Blueprints` 下五个 Blueprint 及 `Content/CGHSim/Maps/L_CGHWorkbench.umap`。关卡保存一组已连接的光学 Actor，Presentation 文件夹中的地板和普通 UE 灯光与光学参数分离。`Materials`、`Meshes` 保留目录，目前显示组件使用引擎自带基础网格。已将 Editor Startup Map 与 Game Default Map 指向该关卡。

本次验证结果：

- `CGHSimEditor Linux Development` 构建成功，包含 UHT 反射代码及 `CinematicCamera` 依赖。
- `CGHSim Linux Development` 构建成功，确认运行时 Actor 不依赖编辑器模块。
- 无界面 Unreal Python 创建并保存场景，重新加载后验证引用保持有效；资源生成最终运行 `0 error(s), 0 warning(s)`。
- `Scripts/verify_cgh_scaffold.py` 通过，日志包含 `CGH_SCAFFOLD_SMOKE_OK`。覆盖 Blueprint 继承、保存的参数和引用、共同平移/旋转后的坐标不变性、标记独立性、SLM 实际网格尺寸、相机焦距/光圈/对焦距离单位同步，以及缺失或重复引用、非法物理参数、非单位缩放的拒绝行为。检查只修改未保存的临时世界。
- 测试日志中的 24 条警告来自故意构造的非法配置；Python 检查为 `0 error(s)`。

资源生成脚本 `Scripts/create_cgh_assets.py` 可重复运行，保留已有 Blueprint 和地图。已实际复跑并比较 SHA-256，六个资源文件均逐字节不变。用法、类职责和单位约定见 [CGH_Actor_Scaffold.md](CGH_Actor_Scaffold.md)。完整执行日志位于本机忽略目录 `Saved/Logs/CGHAssetSetup.log` 和 `Saved/Logs/CGHScaffoldChecks.log`。

尚未验收：Moonlight 中的实际视觉效果、交互式 Simulation、Cook/打包和独立部署。GS/FFT/传播/网格采样、相位纹理、光学重建、V100 通信均未实现；场景与标签明确保持占位状态。


## 11. 场景快照与 SLM 法线方向（2026-09-20 补充）

已扩展 `FCGHSceneDescription`，并在 `ACGHWorkbenchActor` 中实现编辑器和运行时的自动更新。快照采用独立的只读描述结构，`SchemaVersion = 1`；位置以 `CGHSLMActor` 的原点和旋转为参考，所有长度使用米、相位使用弧度，方向为 SLM 局部坐标中的单位向量。

### 11.1 快照字段与单位

| 部分 | 已导出的字段 |
| --- | --- |
| SLM | `ResolutionX/Y`、`PixelPitchXM/YM`、`ActiveWidthM/ActiveHeightM`；有效尺寸由分辨率乘像素间距计算；保留调制类型 |
| Reconstruction Light | `WavelengthM`、`Amplitude`、`InitialPhaseRad`、`DirectionSLM`；另含光源类型、点光源位置 `PositionSLMM`、偏振角 `PolarizationAngleRad` |
| Targets | 每个目标的 `PositionSLMM`、`Amplitude`、`PhaseRad` 和目标类型 |
| Camera | `OpticalPositionSLMM`、`ForwardDirectionSLM`、`FocalLengthM`、`FNumber`、`FocusDistanceM`、`SensorWidthM/SensorHeightM`、`OutputResolutionX/Y` |

Camera 的光学位置和前向来自 `OpticalReference` 组件，包含其相对 Actor 的偏移和旋转。Actor 参数仍保留适合编辑的 nm、um、mm 单位，导出时统一换算为 SI 单位。

### 11.2 有符号位置与 SLM 法线

SLM 局部 **+X 是光学法线**，有效面位于局部 YZ 平面；+Y 为横向，+Z 为纵向。对于世界位置 `P`、SLM 原点 `O`（两者以 UE 厘米表示）及世界空间单位法线 `N`，快照中的深度为：

```text
X_m = dot(P_cm - O_cm, N) × 0.01
```

沿法线一侧为正 X，反向一侧为负 X，平面上为零；Y/Z 同样保留相对局部轴的正负号。固定世界位置时，SLM 法线反向会自动使 X 反号；法线倾斜时按旋转后的轴投影，不使用世界 X 差值或无符号距离。目标、相机光学位置和重建光源位置使用同一约定，光源传播方向与相机前向也随 SLM 坐标轴转换。

转换使用 `InverseTransformPositionNoScale` / `InverseTransformVectorNoScale`，忽略 SLM Actor 缩放，保留真实物理距离。`ValidateScene()` 仍会拒绝非单位光学根缩放；忽略缩放不代表该配置通过物理验证。

### 11.3 更新触发与使用方式

Workbench 暴露临时、只读的 `SceneDescription` 和 `bSceneDescriptionComplete`。编辑器通过 `OnObjectPropertyChanged`、`OnObjectTransacted` 回调更新；组件观察覆盖关联 Actor 的根变换及 Camera 的 `OpticalReference` 变换。引用替换、Actor 销毁和重新绑定均会刷新观察关系并重建快照，防止残留旧值。

运行时通过 `TG_PostUpdateWork` Tick 捕获直接写入参数的变化。需要在同一帧中立即消费新值时，先显式调用 `UpdateSceneDescription()`。缺失引用对应的描述恢复为零值，缺失 SLM 时无法建立局部坐标系，目标列表为空；单个目标引用缺失时保留数组位置并置零。

`bSceneDescriptionComplete` 仅表示 SLM、Camera、Light 和非空目标列表中的所有引用可用。它不代替物理参数验证；调用方需要单独执行 `ValidateScene()` 检查非法参数、重复目标引用及缩放等问题。

保留用户已有的原生默认值：SLM 为 `4096 × 4096`、像素间距 `8 um`，对应 `32.768 × 32.768 mm`；相机对焦距离为 `500 mm`。Smoke 检查已对齐当前默认值，保存关卡中的目标位置按其实际姿态计算预期值，避免继续依赖历史硬编码坐标。本次未写入 Blueprint 或地图资源，也未创建提交。

### 11.4 验证结果与证据

| 验证 | 结果及覆盖范围 |
| --- | --- |
| `CGHSimEditor Linux Development` | 最终法线方向补充后的构建 `Result: Succeeded` |
| `CGHSim Linux Development` | 场景快照功能实现阶段构建 `Result: Succeeded`；法线说明与回归测试补充后未另行重跑 Game 构建 |
| `CGH.SceneDescription` 自动化测试 | 4 项全部通过，进程退出码 0：`EditorChanges`、`RuntimeChangesAndReferences`、`SIUnitsAndOpticalCoordinates`、`SignedPositionsFollowSLMNormal` |
| `Scripts/verify_cgh_scaffold.py` | 输出 `CGH_SCAFFOLD_SMOKE_OK`，进程退出码 0 |

自动化覆盖 SI 换算、相机光学参考、编辑器参数与事务回调、运行时直接参数写入、引用替换/删除，以及编辑器和 Game 世界中的法线前后、平面上、法线反向和倾斜后的有符号位置。事务回调已测试，尚未实际操作编辑器的 Undo/Redo 界面。

本次已有日志已保留至本机忽略目录 `Saved/Logs`，未纳入提交；此次文档更新未重新执行构建或测试：

- `Saved/Logs/CGHSceneDescriptionEditorBuild_2026-09-20.log`
- `Saved/Logs/CGHSceneDescriptionGameBuild_2026-09-20.log`
- `Saved/Logs/CGHSceneDescriptionTests_2026-09-20.log`
- `Saved/Logs/CGHSceneDescriptionSmoke_2026-09-20.log`

源码入口、构建及测试命令见 [CGH_Actor_Scaffold.md](CGH_Actor_Scaffold.md)，当前状态与后续任务见 [开发交接文档](CGHSim_Development_Handoff.md)。

下一步仍需在 Moonlight 中完成场景视觉验收和交互式 Simulation，实际检查 Undo/Redo；Cook/打包及独立部署尚未验收。首个光学算法的范围与接口待讨论，当前没有新增 GS/FFT/传播求解器、相位纹理、光学重建或设备通信实现。


## 12. 网格目标、选择性能与 SLM 相位预览（2026-09-21 补充）

本节补记 2026-09-20 完成的网格目标资源，以及 2026-09-21 完成的选择性能修复和 SLM 相位预览。`FCGHSceneDescription` 当前为 **SchemaVersion = 2**。第 10、11 节中的点目标限制、SchemaVersion 1 和“相位纹理未实现”等描述保留为历史记录，不代表当前实现。

### 12.1 网格目标资源与自动更新

`ACGHTargetActor` 现在同时支持 Point 和 Mesh。Mesh 使用独立的 `GeometryMesh` 组件，Point 继续使用数学点的振幅和相位，忽略网格与采样参数，并清空不再使用的几何、点云数组。

已整理 `FCGHTargetDescription`、`FCGHMeshGeometryResource`、`FCGHPointCloudResource` 和 `FCGHObjectPoint` 的反射结构、声明顺序及默认值。一个目标的描述、网格和点云使用同一个 `ResourceId`，用匹配的 `Revision` 标识同一代数据。ID 属于当前进程中的 Actor 实例；创建另一个实例会得到不同 ID，不能作为跨会话持久资源标识。

| 配置或资源 | 当前行为 |
| --- | --- |
| `GeometryMesh` | 读取 Static Mesh 的 LOD 0 顶点、三角形索引、法线和 UV0；Nanite 资产使用回退网格 |
| `SLM` | 可显式指定；未指定时由关联 Workbench 提供。不同参考坐标系不能混用，同一缓存不能被另一个 Workbench 强行切换 |
| `SliceCount` | 默认 32；沿目标原点指向 SLM 原点的轴等距分层，在每层中点取截面 |
| `PointSpacingMm` | 默认 5 mm；约束缩放后的截面轮廓采样间距 |
| `MaxPointCount` | 默认 100,000；超限时报错并清空结果，不返回不完整的点云 |
| `FCGHObjectPoint` | 包含局部米制位置、插值法线/UV、目标振幅及相位 |

采样对象是三角形与截面的相交轮廓，不填充截面内部。重合点合并，退化三角形跳过；共面网格保留边界并去掉内部三角剖分对角线。该采样尚不处理遮挡、材质光学响应或传播计算。

Construction、BeginPlay、变换/属性/事务通知及 Tick 输入比较负责更新。Actor 缩放、网格组件偏移/旋转/缩放、模型替换、采样参数、振幅/相位和 SLM 参考变化都会发布新版本；输入不变时保持原版本。`UpdateTargetResources()` 用于同帧立即读取，`RebuildTargetResources()` 用于自定义运行时网格修改后的强制重读。失败时通过 `bResourcesValid` 和 `ResourceError` 给出原因，清除旧数组并更新版本，避免继续使用过期数据。

Workbench 从 Actor 复制轻量描述和版本引用，不把大块几何数组重复装进场景快照；目标资源无效或 SLM 参考不匹配时，场景快照不完整。Mesh 目标允许非均匀和镜像缩放，SLM/Camera/Light 的根缩放仍须为 `(1,1,1)`。

### 12.2 点云显示尺度与选择性能

开启 **Show Point Cloud** 会隐藏 `GeometryMesh` 并显示采样点，关闭后恢复网格显示。`DebugPointSize` 表示屏幕像素大小，不代表点的物理尺寸；屏幕上的点标记可能使轮廓看起来更粗。源网格 LOD 0/Nanite 回退几何与视口最终网格也可能存在差别，应同时检查资源是否已刷新。

几何和点云已经包含 Actor/网格组件的实际缩放及组件偏移，单位为目标刚性局部坐标中的米。点云显示组件使用绝对单位缩放，显示时只把米换成厘米并应用目标平移/旋转，不能再次乘 Actor 缩放。非均匀缩放的法线使用逆转置，镜像变换同步修正三角形绕序；已有回归验证其世界坐标重建和法线方向。SLM 物理尺寸同样与屏幕预览尺寸分开：像素间距控制物理有效面，不用于放大预览像素网格。

针对“每次选择目标都很慢”的反馈，确认选择本身没有推进资源版本；开销来自 Details 对大块数组创建属性树。修复后 Details 只显示 `MeshVertexCount`、`MeshTriangleCount`、`PointCount`、描述/版本和资源状态。完整网格和点云仍可通过 C++、Blueprint 和反射读取，但大数组不再展开到 Details，也不写入 Undo 事务。派生描述与状态同样排除事务保存，避免撤销时元数据与缓存内容不同步。

实际编辑器选择和 Details 属性树回归使用 3,264 个缓存点，检查重复选择/取消选择不会重建资源、改变 Revision 或替换缓存内存，且面板仍显示正确的计数。此阶段的 14 项 CGH 自动化测试及 Editor/Game 构建已通过，报告位于 `Saved/Automation/CGHTargetSelection`。

### 12.3 SLM 相位存储与发布接口

新增 `Types/CGHSLMPhasePattern.h` 中的 `FCGHSLMPhasePattern`：`ResolutionX/Y`、`TArray<double> PhaseRad` 和 Actor 管理的 `Revision`。相位单位为弧度，按 `PhaseRad[row * ResolutionX + column]` 存储，row=0 为顶行；行列与 SLM 局部空间轴的对应关系见第 13 节及 [SLM 像素坐标约定](SLM_Pixel_Coordinates.md)。默认 SLM 仍为 `4096 × 4096`、`8 um`，有效面为 `32.768 × 32.768 mm`；默认相位数组为空，不分配整幅零值数据，也不冒充已计算的全息图。

| 接口或状态 | 用途与约定 |
| --- | --- |
| `SetPhasePattern()` | 接受与当前 SLM 分辨率完全一致、数量精确且全部有限的相位数组；成功后由 Actor 持有副本并管理版本，忽略传入 Revision |
| `GetPhasePattern()` / `GetPhasePatternRevision()` | C++ 只读引用访问和版本检查，避免无意复制大数组 |
| `GetPhasePatternCopy()` | Blueprint 获取独立副本；修改副本不能绕过 setter 修改内部数据或跳过版本更新 |
| `HasValidPhasePattern()` | 检查当前数据是否仍匹配当前像素网格 |
| `SynchronizePhasePattern()` | 同帧消费前检查分辨率变化，清除不再匹配的数据；编辑器/运行时 Tick 也执行该检查 |
| `ClearPhasePattern()` | 清空数据并立即释放预览纹理；重复清空未变化的空状态不会增加版本 |
| `GeneratePreviewPhaseRamp()` | 显式生成每行相同的 `2*pi*column/ResolutionX` 测试斜坡，并设置 `bIsPreviewPhasePattern`；不执行 CGH 求解 |

无效输入返回 false，通过 `PhasePatternError` 说明原因，同时保留上一份有效数据和版本。像素间距变化与 **Refresh Visualization** 保留有效相位；任一分辨率变化会清空不匹配的数据并增加版本。相同相位重新发布、重复选择 Actor 不增加版本；外部发布会清除“测试斜坡”标记，即使数据恰好相同。

状态初始为 `GenerationState = NotImplemented`、`bHasPhaseData = false`；成功发布数据后为 `Ready`，这里只表示“已有有效相位数据”，不表示光学求解器已经实现。清空后恢复初始状态。相位数据为 Transient，不随地图保存；内部存储不暴露可变 Blueprint 成员，也不出现在 Details 数组树或 Undo 事务中。

输入上限为每轴 16,384 像素、总计 67,108,864 像素；预览还受显卡最大纹理尺寸限制。较大分辨率需要相应的 CPU/GPU 内存，当前限制不代表所有尺寸均已进行性能验收。

### 12.4 选中 SLM 时的原生相位预览

`UCGHSLMPreviewComponent` 接入 UE 原生 selected-actor picture-in-picture 机制，选中 SLM 即可显示相位面板，不需要 Camera 或 SceneCapture。空状态显示 **No phase pattern**。在 **CGH > Phase** 点击 **Generate Preview Phase Ramp** 可检查显示，面板明确标为 **Preview test ramp**；使用 **Clear Phase Pattern** 返回空状态。

该面板遵循编辑器 **Preview Selected Cameras** 开关，**Camera Preview Size** 调整面板尺寸。完整图像保持像素网格纵横比并按需留边，面板尺寸不会改变 SLM 分辨率、物理像素间距或存储的相位。编译后需重启编辑器以加载新的原生预览组件；现有 `BP_CGHSLM` 可以继承该组件，无须重新生成 Blueprint 资源。

预览纹理与 SLM 分辨率一一对应，使用 BGRA8、最近邻采样和禁用 gamma 转换的线性灰度显示。相位先环绕到 `[0, 2*pi)`：0 和整周期为黑色，pi 为中灰，接近 2*pi 为白色；负相位按同一规则环绕。纹理仅在数据版本或网格尺寸变化后更新，重复选择复用缓存。预览组件与纹理只用于编辑器，运行时仍保留相位数据和发布接口。

### 12.5 本次验证结果

以下记录来自已经执行的构建与测试；此次整理进度文件没有重新构建、运行测试或保存项目资产。

| 验证 | 已确认结果 |
| --- | --- |
| `CGHSimEditor Linux Development` | 构建通过，退出码 0 |
| `CGHSim Linux Development` | 构建通过，退出码 0；运行时存储与编辑器预览依赖分离 |
| NullRHI 无界面 `CGH` 自动化 | **21 项全部通过**，退出码 0；覆盖场景坐标、网格采样/资源版本/选择性能、相位校验/生命周期、原生预览发现与纹理缓存 |
| Vulkan/Slate 实际渲染自动化 | **1 项通过**，退出码 0：`CGH.SLMPhasePattern.RenderedPreviewPreservesGrayscale`；实际渲染 4×2 相位网格，验证 0、64、128、255 灰度和行顺序 |
| 现有 `BP_CGHSLM` 无保存 smoke 检查 | 通过；确认继承启用的原生预览组件，并支持相位发布、读取副本、测试斜坡和清空 |

相位生命周期测试同时覆盖 Game 和 Editor 世界、直接写入分辨率后的 Tick 失效、Details 的属性修改触发、像素间距修改保留相位、无效数据原子拒绝，以及 Blueprint 副本隔离。Vulkan 测试渲染的是实际 Slate 相位控件，补充了无界面测试无法判断的显示灰度与行顺序验证。

报告与截图位于本机 `Saved/Automation`，不作为项目源资源保存：

- `Saved/Automation/CGHSLMPreview`：21 项无界面测试报告。
- `Saved/Automation/CGHSLMPreviewRender`：Vulkan/Slate 渲染测试报告。
- `Saved/Automation/CGHSLMPreview/SLMPhasePreview.png`：实际渲染截图。
- `Saved/Automation/CGHTargetResources`：此前网格目标阶段的 13 项测试报告。
- `Saved/Automation/CGHTargetSelection`：此前选择性能阶段的 14 项测试报告。

本次记录同时将构建/测试日志从临时目录归档到 `Saved/Logs`：`CGHSLMPreviewEditorBuild_2026-09-21.log`、`CGHSLMPreviewGameBuild_2026-09-21.log`、`CGHSLMPreviewTests_2026-09-21.log`、`CGHSLMPreviewRender_2026-09-21.log` 和 `CGHSLMPreviewBlueprintSmoke_2026-09-21.log`。这些日志和自动化报告是本机忽略文件，不随 Git 提交。

上述检查未保存或覆盖用户的 Blueprint、网格资产和地图。原先 `verify_cgh_scaffold.py` 的保存关卡部分仍以初始场景默认值为前提；用户已经调整场景时，不能把该旧 fixture 的默认值假设当作当前场景配置要求。

### 12.6 尚未完成与继续开发入口

GS/FFT、光学传播、真实全息生成、重建图像/传感器模拟，以及外部求解器或 V100/显示设备通信仍未实现。当前相位存储、测试斜坡和灰度预览为后续算法提供输入输出接口，不等同于光学仿真结果。

Cook/打包和独立部署尚未验收。打包后采样源网格需预先启用 **Allow CPU Access** 并确保 LOD 0 常驻；这些设置在目标打包环境下仍需单独验证。已完成的 Vulkan 控件渲染测试不代表所有 GUI 操作、Moonlight 中的完整场景视觉效果、长时间交互式 Simulation 或实际 Undo/Redo 流程均已人工验收。

下一步可在当前编辑器中确认 SLM 选择预览及测试斜坡交互，再明确首个光学求解器的输入、输出和更新时机。最新接口、构建/测试命令及预览用法见 [CGH_Actor_Scaffold.md](CGH_Actor_Scaffold.md)，整体续接状态见 [开发交接文档](CGHSim_Development_Handoff.md)。


### 12.7 预存相位样例与编辑器启用（2026-09-21）

新增 `UCGHPhasePatternAsset`，将相位数组作为独立 DataAsset 持久保存，同时保持大数组不在 Details 中展开。已实际创建 `Content/CGHSim/PhasePatterns/DA_SLMPreviewPattern.uasset`，包含 256×256 的水平/垂直灰度斜坡、同心环、棋盘格和左上角亮色方向标记，便于观察预览方向与灰度变化。

重新编译并重启编辑器后，选择 SLM，在 **CGH > Phase** 点击 **Load Stored Phase Pattern**。**Stored Phase Pattern** 默认指向该样例；启用时使用最近邻采样匹配当前 SLM 像素网格，不改变分辨率、像素间距或物理有效面积。**Clear Phase Pattern** 清除当前相位，保留已保存的样例与引用，可随时重新加载。样例资产会持久保存，Actor 当前相位仍为 Transient；样例不是光学求解结果。`Scripts/create_cgh_phase_sample.py` 可重建缺失的样例，已有资产不会覆盖。

本次 Editor/Game 构建与 **26 项无界面 CGH 测试全部通过，退出码均为 0**。新增验证覆盖资产序列化、数据所有权、重采样、加载/清空/重新加载、失败时保留旧有效相位，以及物理参数保持不变。独立 Unreal 进程从磁盘重新加载样例，验证现有 `BP_CGHSLM` 默认引用、32×16 网格启用，以及原生默认 4096×4096 网格启用。重复执行生成脚本后样例字节不变；哈希检查确认原有 10 个资产/地图均未改变，只新增了相位样例资产。

最新报告：`Saved/Automation/CGHStoredPhasePattern`。构建、资产生成与 smoke 日志：`Saved/Logs/CGHStoredPhase*2026-09-21.log`。此前 Vulkan/Slate 测试仍作为未改变的灰度绘制路径的实际渲染证据；本次没有重新声明完整 GUI、光学算法或打包部署已验收。

## 13. SLM 像素坐标约定与代码审查（2026-09-21）

以 [SLM 像素坐标约定](SLM_Pixel_Coordinates.md) 为权威记录，统一相位数组、二维图像与 SLM 局部空间之间的关系。保留已有公开字段名，X/Y 后缀表示图像/像素网格轴，不直接表示 Actor 的空间 X/Y 轴。

| 网格字段 | 图像含义 | SLM 局部空间含义 |
| --- | --- | --- |
| `ResolutionX`、`PixelPitchXM`（编辑参数 `PixelPitchXUm`） | 水平列数与间距 | 横向沿 Y，column 增加时向 +Y |
| `ResolutionY`、`PixelPitchYM`（编辑参数 `PixelPitchYUm`） | 垂直行数与间距 | 纵向沿 Z，row 增加时向 -Z |

SLM 局部 +X 为光学法线，+Y 为规范图像中的右方，+Z 向上。像素中心位于 X=0 平面，原点位于整幅有效面的中心：

```text
PhaseRad[row * ResolutionX + column]
column ∈ [0, ResolutionX - 1]
row    ∈ [0, ResolutionY - 1]

X = 0
Y = (column - (ResolutionX - 1) / 2.0) * PixelPitchXM
Z = ((ResolutionY - 1) / 2.0 - row) * PixelPitchYM
```

中心坐标使用米；公式必须采用浮点除法，避免偶数分辨率发生半像素偏移。row=0 位于上侧，column=0 位于左侧；有效宽高仍为分辨率乘间距，边缘距离最外侧像素中心半个像素间距。

规范正面图保留用户指定的“从 +X 一侧朝 -X 看，+Y 显示向右，+Z 显示向上”，将其明确定义为二维图像显示约定。普通 UE 相机从 +X 一侧朝 -X 看、并保持 +Z 朝上时，其屏幕右方实际为 SLM 的 -Y；要让该相机图像匹配规范正面图，需水平镜像。这一点由本地 UE 旋转矩阵和相机视图矩阵确认。当前相位控件直接绘制数组，场景中的普通相机仍保持物理视角；本次不翻转相位数据或修改 Actor 坐标轴。

源码审查确认：相位存储、灰度上传/绘制及最近邻重采样保持行列顺序；有效面的宽度沿局部 Y、高度沿局部 Z；SI 场景导出使用相同的 SLM 刚性局部坐标。未发现这些路径存在轴交换或行方向翻转。当前尚无像素中心光学求解器，也没有将相位贴到世界空间 SLM 网格的实现；后续实现必须遵守上述中心公式，并单独处理物理相机与规范图像的镜像关系。

本次更新记录和相关头文件注释，明确行列索引、空间轴与公开字段的命名含义；没有改变运行行为或保存任何资产。既有测试结果仍按第 12 节对应日期记录，本次检查结果另行补记。

本次验证：Editor Linux Development 构建通过，**26 项无界面 CGH 自动化测试全部通过**，退出码 0，测试失败、警告及未运行数量均为 0。报告位于 `Saved/Automation/CGHSLMCoordinates/index.json`，构建/测试日志为 `Saved/Logs/CGHSLMCoordinates*2026-09-21.log`；文档链接及 `git diff --check` 检查通过。本次未增加测试或重跑真实 RHI 渲染；像素中心/相机映射尚无测试，既有渲染样本的 180 度对称性无法检出同时水平与垂直翻转，该覆盖缺口已在规范页记录。


## 14. CPU 单点聚焦参考求解器（2026-09-21）

本节记录首个光学计算实现；第 12、13 节中的“求解器未实现”和“像素中心尚无数值消费者”描述保留为对应时间的历史记录。当前接口、使用步骤与完整假设见 [CPU PointFocus 求解器](CGH_PointFocus_Solver.md)。

### 14.1 传播约定与数值范围

明确将传播符号写入求解输入：`U(r) ∝ exp(+i*k*r)`，`k = 2*pi/WavelengthM`。平面波入射相位为 `phi_inc(p)=InitialPhaseRad+k*dot(DirectionSLM,p)`，每个像素输出 `wrap_[0,2*pi)(TargetPhaseRad-k*r-phi_inc(p))`，因此叠加入射相位和传播相位后，目标处相位均等于目标相位模 `2*pi`。结果元数据同样携带传播约定，Actor 拒绝与请求不一致的结果。未来 CUDA、FFT 或网络库必须保留该约定，或在边界处明确转换，不能默默改变符号。

像素中心严格采用第 13 节公式：X=0，Y=`(column-(ResolutionX-1)/2.0)*PixelPitchXM`，Z=`((ResolutionY-1)/2.0-row)*PixelPitchYM`，按 `PhaseRad[row*ResolutionX+column]` 存储。计算使用米、弧度和 `double`；用 `std::hypot` 求三维距离，避免不必要的平方中间溢出。

第一版只接受一个 `TargetType=Point` 的 `ACGHTargetActor` 和 phase-only SLM。目标必须位于 SLM 平面外，即局部 X 不为零；正负 X 均可作为有符号输入，当前标量距离模型没有选择物理器件的出光侧。分辨率、间距和波长须合法且正值，位置/相位/传播计算须有限；Mesh、多目标及非法输入返回失败，不发布部分数组。

第一版只支持 **PlaneWave**，读取光源波长、`InitialPhaseRad` 和单位传播方向 `DirectionSLM`，补偿斜入射在 SLM 上形成的相位斜坡。初相位参考点固定为 **SLM 原点**；光源 `PositionSLMM` 不参与计算，移动光源 Actor 不会改变参考点。沿 ±X 法向入射且初相位为零时，公式退化为原来的 `wrap(TargetPhaseRad-k*r)`。PointSource 或未知光源类型明确失败。

初相位和方向各分量必须有限，方向非零且满足 `abs(DirectionSLM.SizeSquared()-1)<=1e-6`。光源 Amplitude 必须有限且非负，PolarizationAngleRad 必须有限；这两个参数虽被校验，但不影响当前标量 phase-only 结果。目标振幅/旋转、相机参数也不参与计算；没有 `1/r` 振幅权重、偏振响应模型、传播图或传感器模拟。PointFocus 不要求 Camera 引用，但 Workbench 的全场景完整性/验证仍包含 Camera。

### 14.2 Actor、后端与缓冲区

新增 `ACGHSolverActor`，默认 **CPU / PointFocus**，按钮 **Generate Phase Pattern** 和 **Cancel Solve**，可选 **Auto Solve** 默认关闭。作业状态为 `Idle`、`Queued`、`Running`、`Ready`、`Failed`，并暴露请求编号、状态消息和计算耗时。自动模式只响应实际参与计算的输入或引用变化，包括光源初相位、方向和类型；合法的光源位置/振幅/偏振改变不触发重算，非法字段仍会在提交和发布前校验中被拒绝。取消后不会立即重算未变输入。临时缺失的引用恢复后可自动重试，未变的非法数值（包括 NaN）不会反复创建任务。

数据流为 UE Actor → Workbench 的 `FCGHSceneDescription` → `FCGHSolverInput` 快照 → `UCGHSolverBackend` → `FCGHSolverResult/FCGHSLMPhasePattern` → `SetPhasePattern()` → SLM 预览。`UCGHCPUSolverBackend` 在 UE 线程池执行纯数值核心；worker 持有不可变的自有输入副本、取消/完成原子标志和输出缓冲，不访问任何 Actor/UObject。

每个 Solver 最多有一个运行任务和一个最新待处理请求。新请求替换待处理输入并协作取消旧任务，等待旧 worker 实际结束后再启动下一个，避免同时分配多幅输出。Actor 在编辑器和运行时 Tick 非阻塞轮询；读取结果前检查完成标志，开始待处理任务和发布结果前核对输入、Actor 引用及 SLM 相位版本，防止目标移动、参数修改、引用替换/删除或人工加载/清空相位后被旧结果覆盖。

`EndPlay` 取消并释放任务、恢复 `Idle`；后端与作业元数据使用 `DuplicateTransient`，复制 Actor 和 PIE 副本不会继承旧任务或状态。成功结果在 game thread 通过新增的 C++ move setter 校验后交给 SLM，避免复制整幅计算结果。取消、失败或过期结果不覆盖已有相位；SLM 分辨率修改仍按原规则清空不匹配数据。数值循环已经异步，但快照、相位校验、灰度转换和纹理上传仍在 game thread，大分辨率预览仍可能消耗帧时间，不能据此宣称整个 UI 更新无停顿。

Workbench 新增可选 `Solver` 引用、**Solve Phase Pattern** / **Cancel Solve** 按钮及状态镜像。Solve 会给尚未绑定的 Solver 指定当前 Workbench，不覆盖已经绑定的其他 Workbench。作业和缓冲始终归 Solver Actor 管理；Workbench 负责快照与用户命令入口。Docker 枚举目前明确返回未实现错误，没有创建网络客户端或进行 TCP 通信。

### 14.3 现有关卡使用与资源保护

重新编译并重启编辑器后，从 Place Actors/C++ 类浏览器添加原生 **CGH Solver Actor**，将其 **Workbench** 指向现有工作台。工作台指定一个平面外的 Point 目标、SLM 与 PlaneWave 重建光源，设置波长、初相位和朝向；可选将工作台 **Solver** 反向引用也设置好。点击 Solver 的 **Generate Phase Pattern** 或 Workbench 的 **Solve Phase Pattern**，状态到 `Ready` 后选择 SLM 查看灰度相位。需要随目标位置/相位、像素网格/间距或光源波长/初相位/方向/类型变化重算时，再打开 **Auto Solve**。

`Ready` 表示最后接受的相位已发布，不表示已经生成光学重建图像或验证实际器件聚焦。手动模式下之后修改场景仍需重新 Generate。相位继续为 Transient，关卡重开后需要重新生成。

`Scripts/create_cgh_assets.py` 已增加缺失 `BP_CGHSolver` 的创建项，以及仅在新建地图时连接 Workbench/Solver 双向引用；旧地图继续原样保留，不会自动插入 Actor。本次没有执行该生成器，也没有保存 Blueprint 或地图资源。

### 14.4 验证记录

最终 Editor/Game Linux Development 构建及 **42 项无界面 CGH 测试全部通过**，退出码 0，测试失败、警告及未运行数量均为 0。其中 6 项为 PointFocus 数值测试，10 项为 Solver Actor/生命周期测试。独立复场传播验证入射平面波相位、SLM 相位与 +kr 叠加后达到目标相位；覆盖斜入射、旋转后的 SLM 坐标、奇偶/非正方形网格、不同间距、输入拒绝、取消/替换、过期结果保护、自动更新/恢复、move 发布、Actor 复制和退出生命周期。

无保存 4096×4096 斜入射、非零初相位求解通过：单次无界面运行测得提交 0.111 ms、worker 计算 0.548120 s、最大轮询/发布调用 22.267 ms；取消另一幅大网格任务返回耗时 0.053 ms。现有 `BP_CGHSLM` 的 32×16 计算结果启用也通过。11 个已有资产/地图的哈希完全不变。报告位于 `Saved/Automation/CGHPointFocus/index.json`，同目录保留 `verify_default_grid.py`；构建、测试和 smoke 日志为 `Saved/Logs/CGHPointFocus*2026-09-21.log`。这些计时仅来自单次 NullRHI 检查，不含灰度转换/纹理上传与真实 GUI 响应。

完整 GUI 响应、默认 4096×4096 网格的实际交互体验、光学重建、Simulation、Cook/打包仍须分别验收。后续 Docker/TCP 后端可复用现有输入输出与作业接口，并以 CPU 结果作为数值对照。
